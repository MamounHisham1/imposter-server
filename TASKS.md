adding timer in each phase (hints (each player gets 20s max then it moves to the next player), votes (30s per round), keep playing)

leader should be able to kick players

the voting/keep-playing phase should be by votes (like currently the leader only who decides to continue typing hints or to start voting)
